{
  "name": "metube-backend",
  "version": "1.0.0",
  "description": "\"This the backend of my metube project\"",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "build" : "node server.js",
    "nodemon": "nodemon --exec 'esbuild --platform=node --outdir=dist --entry-point=src/server.js --watch --bundle'"
  },
  "author": "Udofia Unwana",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "body-parser": "^1.20.2",
    "bootstrap": "^5.3.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "esbuild": "^0.20.0",
    "esm": "^3.2.25",
    "express": "^4.18.2",
    "ibm-cos-sdk": "^1.13.3",
    "mongoose": "^8.1.0",
    "nodemailer": "^6.9.8",
    "nodemon": "^3.0.3"
  }
}
